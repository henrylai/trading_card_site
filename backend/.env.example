# Port for the Express server (Railway sets this automatically)
PORT=3001

# CORS allowed origin — set this to your frontend URL after deploying
FRONTEND_URL=https://your-frontend.vercel.app

# Optional: set to 'true' to enable verbose request logging
DEBUG=false
